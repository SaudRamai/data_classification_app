# Ensure consistent line endings
* text=auto

# Shell scripts must use LF (important on Windows)
*.sh text eol=lf

# Docker-related files typically LF
Dockerfile text eol=lf
docker-compose.yml text eol=lf

# Markdown, YAML, TOML as text
*.md text
*.yml text
*.yaml text
*.toml text

# Python files as text (git will normalize)
*.py text
